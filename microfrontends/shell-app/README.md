# shell-app (archived)

Historical shell prototype that does not consume the remote applications. It is unsupported and excluded from the root build and CI. See `../README.md` and use the root React application for active development.
