# Optional Compose overrides. Copy to .env and replace these values.
APP_PORT=8080
POSTGRES_PASSWORD=replace-with-a-local-database-password
JWT_SECRET=replace-with-at-least-32-random-characters

# Required for GitHub analytics. Use a fine-grained, least-privilege token.
GITHUB_TOKEN=
